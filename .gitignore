node_modules
.next
out
dist
.DS_Store
*.log
docs/planning/
docs/Claude outputs/
Claude outputs/

# Source material for the About page: narrative blueprint, drafts and the ChatGPT prototypes.
# Names former colleagues and discusses compensation — stays local, the repo is public.
app/about/reference/

# Narration tooling: the local Python environment and cached model/alignment output
docs/narration/.venv/
docs/narration/incoming/
docs/narration/.cache/
docs/narration/for-elevenlabs*.txt
docs/narration/__pycache__/

# The About page's layout lab (/about/lenses-lab): kept permanently on this machine as a reference
# for layouts not chosen — never delete it — but it stays out of the public repo and the live site.
app/about/lenses-lab/

# The homepage layout lab (/home-lab): four directions for the homepage, local-only like the lenses lab.
app/home-lab/

# The About page loading-curtain lab (/about/loader-lab): local-only like the other labs.
app/about/loader-lab/
# The phone-menu lab (/menu-lab): four directions for the expanded hamburger menu, local-only like the other labs.
app/menu-lab/
