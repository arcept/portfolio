// Table cells that carry a tone (a red or green figure). Shared by the page and by each language's dictionary.
export const bad = (v) => ({ v, tone: 'bad' });
export const good = (v) => ({ v, tone: 'good' });
