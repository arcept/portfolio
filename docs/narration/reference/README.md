Reference only. `narration-player.html` is the working single-file prototype (open it in a browser and press play).
The screenshots were taken in a sandbox with no web-font access, so fonts show as fallbacks; the real page uses
Newsreader / Archivo / IBM Plex Mono. The implementation is vanilla JS + CSS built for a single-HTML artifact:
treat it as a behavioural spec, not code to paste into a component library.
